# Initial discovery and resolved failures

These are transcribed tool observations from 2026-09-11 IST, before the timestamped command recorder was available. They are not fabricated PASS outputs. Application checks use the raw recorded logs and commands.jsonl.

- `Get-ChildItem -Force` and `rg --files --hidden` in D:\Samudra 3D: only three references, phase-prompts and tmp/source-analysis; no app/data/test/backend files.
- `git rev-parse --show-toplevel`: exit 1, `fatal: not a git repository (or any of the parent directories): .git`.
- `Test-Path D:\AGENTS.md`: False; root/descendant AGENTS.md search: none.
- `node --version`: v24.18.0; `npm --version`: 11.16.0; `python --version`: Python 3.14.6.
- Both PATH Python and bundled Python probes for `import fitz` failed with `ModuleNotFoundError: No module named 'fitz'`. No installation attempted. Bundled `pypdf`, `pypdfium2`, `PIL`, `pdfplumber` available; PDFium used successfully instead. `Get-Command pdftoppm, soffice` returned no installed PATH renderer.
- First `& 'C:\Users\ABHINAV\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts/audit-sources.py`: exit 1, slide image lookup `KeyError` because one blip had no embedded reference. Fixed optional relationship handling; second run exit 0 at 10:34:32 IST, 36 entry files, 26 text files, 14 PDF pages and matching original source hashes. All seven slide image references printed and all media saved.
- Initial `npm view vite version engines --json`, `npm view @vitejs/plugin-react version engines peerDependencies --json`, `npm view tailwindcss@3 version --json` in sandbox failed with ENOTCACHED and unwritable external npm-log location. Registry checks were retried with approved network access. Verified Vite 8.3.0, plugin-react 6.1.1 engines ^20.19.0 or >=22.12.0; plugin Vite peer ^8.0.0; compiler-related peers optional. Verified React 18.3.1, Tailwind 3.4.17, PostCSS 8.5.28, autoprefixer 10.5.6, ESLint 9.39.5, hooks 7.1.1 and Playwright 1.63.0. Actual installed metadata is fully preserved by check-environment.mjs output.
- First recorded production build exited 1 but the original recording wrapper did not retain native stderr. Direct `npm.cmd run build` in frontend reproduced: `failed to load config ... vite.config.js`, `[plugin externalize-deps] Error: spawn EPERM`, originating in Vite `optimizeSafeRealPathSync` / Windows child process. Retried outside sandbox with approval; no source workaround. Wrapper corrected to continue collecting native stderr.
- First `versions` command's final `require('@vitejs/plugin-react/package.json')` was unsuccessful (package has restricted exports). Replaced with explicit installed package-file reads in check-environment.mjs, which also asserts all pins against lockfile. No runtime version was inferred from the failed subcommand.
- Initial lint ran slowly because local npm cache was under frontend; added `.npm-cache/**` ignore. Lint then reported four `no-undef` uses of `getComputedStyle` in browser tests. Changed to `window.getComputedStyle`; subsequent lint exit 0. Original logs retained.

These failures are resolved checks/tooling issues, not omitted required acceptance checks. See final command logs for actual subsequent results.
